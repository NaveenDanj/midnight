struct Person {
    var string first_name;
    var string last_name;

    func greet() string {
        return first_name + last_name;
    }

}

func add(int a , int b) int {
    return a + b;
}

func main() int {

    const int person1 = Person{
        first_name = add(25 , 25),
        last_name = "Hettiwaththa"
    };

    var int total = 100;
    total = total + 50 + add(100 , 200);
    add(100 , 200);
    return total;
    
}

