import "std.io.file";

var File file = File{};
var Input input = Input{};

var string path = "/tmp/naveen.txt";
var bool isExists = file.exists(path);

if(isExists) {
    print("File is exists");
}else {
    print("File does not exists");
    file.write(path, "hello world naveen");
}

var string name = "naveen";
var string name2 = "naveen";

if(name == name2) {
    print("Naveen");
}else{
    print(name);
    print("Not Naveen");
}