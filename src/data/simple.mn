var int x = 100;
var int y = 200;
var int total = x + y;
total = total * total;
var float PI = 3.14;
var bool is_true = true;

struct Position {
    var int x;
    var int y;
}

struct Velocity {
    var float vx;
    var float vy;
}

struct GameObject {
    var Position pos;
    var Velocity vel;
}

if (is_true) {
    print(total + 120 - 6579);
    print("");
} else {
    print(x);
    print("");
}

print("hello world");
print(is_true);
print("");

var float total2 = 100;

print(total2);
print("");

if (false) {
    print("total is greater than 90");
}else{
    print("total is less than 90");
}