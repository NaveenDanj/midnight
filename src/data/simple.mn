var int x = 100;
var int y = 200;
var int total = x + y;
total = total * total;
var float PI = 3.14;
var bool is_true = true;

struct Position {
    var int x;
    var int y;
}

struct Velocity {
    var float vx;
    var float vy;
}

struct GameObject {
    var Position pos;
    var Velocity vel;
}

if (is_true) {
    print(total);
} else {
    print(x);
}


var GameObject car = GameObject{
    
    pos = Position{
        x = 90,
        y = 23
    },

    vel = Velocity{
        vx = 45,
        vy = 100
    }
    
};

print("\nhello world");

var float total2 = 100.90 / 67;

print(total2);

if (false) {
    print("\ntotal is greater than 90");
}else{
    print("\ntotal is less than 90");
}